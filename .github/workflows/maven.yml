# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]

jobs:
#  validate:
#    runs-on: ubuntu-latest
#    if: github.event_name == 'push'
#    steps:
#      - name: Check for previous workflow run
#        run: |
#          set -e
#          status=$(curl -s https://api.github.com/repos/${{github.repository}}/actions/runs/${{github.run_id}} | jq -r '.status')
#          if [ "$status" == "completed" ]; then
#            echo "Previous workflow run has completed"
#            echo "status value: $status"
#          else
#            echo "Previous workflow run still in progress"
#            echo "status value: $status"
#            exit 1
#          fi

  env-setup:
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.sha.outputs.SHA }}
    steps:
      - name: Get SHA
        id: sha
        run: echo "SHA=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

#  version-update:
#    needs: env-setup
#    concurrency: 1
#    uses: BreweriesFest/brewery-app/.github/workflows/version.yml@main
#    with:
#      sha: ${{ needs.env-setup.outputs.sha }}

  inventory-service:
    needs: env-setup
    uses: ./.github/workflows/build.yml
    with:
      service-name: 'inventory-service'
      sha: ${{ needs.version-update.outputs.sha }}

  beer-service:
    needs: env-setup
    uses: ./.github/workflows/build.yml
    with:
      service-name: 'beer-service'
      sha: ${{ needs.version-update.outputs.sha }}

  order-service:
    needs: env-setup
    uses: ./.github/workflows/build.yml
    with:
      service-name: 'order-service'
      sha: ${{ needs.version-update.outputs.sha }}

  scheduler:
    needs: env-setup
    uses: ./.github/workflows/build.yml
    with:
      service-name: 'scheduler'
      sha: ${{ needs.version-update.outputs.sha }}